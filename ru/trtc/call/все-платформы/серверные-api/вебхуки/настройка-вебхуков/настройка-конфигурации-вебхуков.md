# Настройка конфигурации вебхуков

## Описание функции

Администраторы приложений могут устанавливать конфигурации обратного вызова через этот API. Если это не первый раз установки конфигурации, новая конфигурация перезапишет старую.

> **Внимание:** Если вы используете устаревшие интерфейсы `TUICallKit.call()` или `TUICallKit.groupCall()` для инициирования вызова, пожалуйста, проверьте папку **Deprecated Document**. Если у вас есть вопросы, вы можете связаться с нами: info_rtc@tencent.com.

## Описание вызова API

### Пример URL запроса

```
https://xxxxxx/v4/call_config/set_callback?sdkappid=88888888&identifier=admin&usersig=xxx&random=99999999&contenttype=json
```

### Параметры запроса

В следующей таблице приведены только параметры, затронутые при вызове этого API, и их описания. Для получения дополнительных сведений о параметрах см. [Введение в REST API](https://www.tencentcloud.com/document/product/647/68926#).

| Параметр | Описание |
| --- | --- |
| xxxxxx | Выделенное доменное имя, соответствующее стране/региону, где находится SDKAppID: Китай: `console.tim.qq.com` Сингапур: `adminapisgp.im.qcloud.com` Сеул: `adminapikr.im.qcloud.com` |
| v4/call_record_http_srv/get_record_by_callid | API запроса |
| sdkappid | Назначенный SDKAppID в консоли Chat при создании приложения |
| identifier | Должен быть учётной записью администратора приложения. Для получения дополнительной информации см. [Администратор приложения](https://trtc.io/document/33517) |
| usersig | Сгенерированная подпись учётной записи администратора приложения. Для получения конкретных инструкций см. [Генерация UserSig](https://trtc.io/document/34385) |
| random | Введите случайное 32-битное целое число без знака. Значение варьируется от 0 до 4294967295 |
| contenttype | Формат запроса имеет фиксированное значение `json` |

### Максимальная частота вызовов

200 раз в секунду.

### Пример пакета запроса

**Базовая форма**

```
{  "Url":"http://www.example.com/callback",  "CallbackCommandList":[    "Call.CallbackAfterStartCall",    "Call.CallbackAfterEndCall",    "Call.CallbackAfterMemberChanged"  ]}
```

### Поля пакета запроса

| Поле | Тип | Обязательно | Описание |
| --- | --- | --- | --- |
| Url | String | Да | Адрес обратного вызова должен начинаться с `http/https`. Рекомендуется использовать более безопасный `https` |
| CallbackCommandList | Array | Да | Слова команд, которые могут запустить обратный вызов, см. [Список слов команд обратного вызова](https://www.tencentcloud.com/document/product/647/68934#) |

### Пример пакета ответа

**Базовая форма**

```
{  "ActionStatus": "OK",  "ErrorInfo": "",  "ErrorCode": 0,  "RequestId": "Id-8c9858f01e954611ae2d4c1b1ed7d583-O-Seq-52720"}
```

### Поля пакета ответа

| Поле | Тип | Описание |
| --- | --- | --- |
| ActionStatus | String | Результат обработки запроса. OK указывает на успешную обработку. FAIL указывает на ошибку |
| ErrorInfo | String | Сообщение об ошибке |
| ErrorCode | Integer | Код ошибки, 0 указывает на успех, ненулевое значение указывает на ошибку |
| RequestId | String | Уникальный идентификатор запроса. Возвращается для каждого запроса. RequestId требуется для локализации проблемы |

## Описание кодов ошибок

Если не возникает сетевая ошибка (например, ошибка 502), код возврата HTTP этого API равен 200. Фактический код ошибки и информация об ошибке указаны в полях ErrorCode и ErrorInfo в теле пакета ответа.

Для общих кодов ошибок (60000–79999) см. документ [Код ошибки](https://www.tencentcloud.com/document/product/647/54901#).

Частные коды ошибок этого API следующие:

| Код ошибки | Описание значения |
| --- | --- |
| 100001 | Внутренняя ошибка сервера. Пожалуйста, повторите попытку |
| 100002 | Неверный параметр. Проверьте, правильно ли составлен запрос в соответствии с описанием ошибки |


---
*Источник: [https://trtc.io/document/68938](https://trtc.io/document/68938)*

---
*Источник (EN): [setting-webhooks-configuration.md](./setting-webhooks-configuration.md)*

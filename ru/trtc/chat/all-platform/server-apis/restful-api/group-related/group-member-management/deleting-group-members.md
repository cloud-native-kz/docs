# Удаление членов группы

## Обзор функции

Этот API используется администраторами приложения для удаления членов группы.

## Описание вызова API

### Применимые типы групп

| ID типа группы | Поддержка RESTful API |
| --- | --- |
| Private | Да. Аналогично рабочим группам (Work) в новой версии. |
| Public | Да |
| ChatRoom | Да. Аналогично группам совещаний (Meeting) в новой версии. |
| AVChatRoom | Нет |
| Community | Да |

Это предустановленные типы групп в Chat. Дополнительную информацию см. в разделе [Система групп](https://intl.cloud.tencent.com/document/product/1047/33529).

> **Примечание** Аудиовидео-группы (AVChatRoom) не поддерживают удаление членов группы. При попытке удалить членов аудиовидео-группы будет возвращена ошибка 10004. Для достижения эффекта удаления членов группы администраторы могут [пакетно отключать и включать членов группы](https://intl.cloud.tencent.com/document/product/1047/34951).

### Пример URL запроса

```
https://xxxxxx/v4/group_open_http_svc/delete_group_member?sdkappid=88888888&identifier=admin&usersig=xxx&random=99999999&contenttype=json
```

### Параметры запроса

В следующей таблице описаны измененные параметры при вызове этого API. Для других параметров см. [Обзор RESTful API](https://intl.cloud.tencent.com/document/product/1047/34620).

| Параметр | Описание |
| --- | --- |
| xxxxxx | Доменное имя, соответствующее стране/региону, где находится ваш SDKAppID. Китай: `console.tim.qq.com` Сингапур: `adminapisgp.im.qcloud.com` Сеул: `adminapikr.im.qcloud.com` Токио: `adminapijpn.im.qcloud.com` Франкфурт: `adminapiger.im.qcloud.com` Кремниевая долина: `adminapiusa.im.qcloud.com` Джакарта: `adminapiidn.im.qcloud.com` |
| v4/group_open_http_svc/delete_group_member | API запроса |
| sdkappid | SDKAppID, назначенный консолью Chat при создании приложения |
| identifier | Учетная запись администратора приложения. Дополнительную информацию см. в разделе **Администратор приложения** в [Аутентификация входа](https://intl.cloud.tencent.com/document/product/1047/33517). |
| usersig | Подпись, сгенерированная учетной записью администратора приложения. Дополнительную информацию см. в разделе [Создание UserSig](https://intl.cloud.tencent.com/document/product/1047/34385). |
| random | Случайное 32-битное целое число без знака в диапазоне от 0 до 4294967295. |
| contenttype | Формат запроса, который должен быть `json`. |

### Максимальная частота вызовов

200 вызовов в секунду

### Пример запроса

- **Базовый формат**
Используется для удаления членов группы. Один запрос поддерживает удаление до 100 членов группы. По умолчанию серверная часть отправляет системные уведомления об удалении членов группы всем членам группы, кроме приватных групп (аналогично рабочим группам в новой версии), которые не были активированы.
Даже если удаляемый пользователь не находится в группе, API все равно вернет успех.

```
{  "GroupId": "@TGS#2J4SZEAEL", // Целевая группа (обязательно)  "MemberToDel_Account": [ // Список членов группы для удаления. Может быть удалено максимум 100 членов.      "tommy",      "jared"  ]}
```

- **Тихое удаление**
Если `Silence` установлено на `1`, система не будет уведомлять никого после успешного удаления члена.

```
{  "GroupId": "@TGS#2J4SZEAEL", // Целевая группа (обязательно)  "Silence": 1, // Удалить ли членов без уведомления (опционально)  "MemberToDel_Account": [ // Список членов группы для удаления. Может быть удалено максимум 100 членов.      "tommy",      "jared"  ]}
```

- **Указание причины удаления**
Укажите причину удаления пользователя из группы, которая будет отражена в отправленном уведомлении.

```
{  "GroupId": "@TGS#2J4SZEAEL", // Целевая группа (обязательно)  "Reason": "kick reason", // Причина удаления пользователя из группы (опционально)  "MemberToDel_Account": [ // Список членов группы для удаления. Может быть удалено максимум 100 членов.      "tommy",      "jared"  ]}
```

### Поля запроса

| Поле | Тип | Обязательно | Описание |
| --- | --- | --- | --- |
| GroupId | String | Да | ID целевой группы |
| Silence | Integer | Нет | Использовать ли тихое удаление. `0`: Нет (значение по умолчанию); `1`: Да. Тихое удаление означает, что никакие члены группы, кроме удаляемого члена, не будут уведомлены об удалении члена группы. |
| Reason | String | Нет | Причина удаления пользователя из группы |
| MemberToDel_Account | Array | Да | Члены группы для удаления |

### Пример ответа

```
{    "ActionStatus": "OK",    "ErrorInfo": "",    "ErrorCode":0}
```

### Поля ответа

| Поле | Тип | Описание |
| --- | --- | --- |
| ActionStatus | String | Результат запроса. `OK`: Успешно; `FAIL`: Ошибка |
| ErrorCode | Integer | Код ошибки. `0`: Успешно; другие значения: Ошибка |
| ErrorInfo | String | Информация об ошибке |

## Коды ошибок

Код состояния HTTP, возвращаемый этим API, всегда 200, если не возникает ошибка сети (например, ошибка 502). Конкретный код ошибки и детали можно найти в полях ответа `ErrorCode` и `ErrorInfo` соответственно.
Для общих кодов ошибок (60000–79999) см. [Коды ошибок](https://intl.cloud.tencent.com/document/product/1047/34348).
В следующей таблице описаны коды ошибок, специфичные для этого API:

| Код ошибки | Описание |
| --- | --- |
| 10002 | Внутренняя ошибка сервера. Повторите попытку. |
| 10003 | Неверное слово команды. |
| 10004 | Неверный параметр. Проверьте описание ошибки и устраните проблему. |
| 10007 | Нет прав на выполнение операции. Например, обычный член публичной группы пытается удалить других пользователей из группы, но это может сделать только администратор приложения. |
| 10010 | Группа не существует или была удалена. |
| 10015 | Неверный ID группы. Используйте правильный ID группы. |

## Инструмент отладки API

Используйте [инструмент онлайн-отладки RESTful API](https://tcc.tencentcs.com/im-api-tool/index.html#/v4/group_open_http_svc/delete_group_member) для отладки этого API.

## Ссылки

Добавление членов группы ([v4/group_open_http_svc/add_group_member](https://intl.cloud.tencent.com/document/product/1047/34921))

## Возможные вебхуки

[После того как пользователь покидает группу](https://intl.cloud.tencent.com/document/product/1047/34373)


---
*Источник: [https://trtc.io/document/34949](https://trtc.io/document/34949)*

---
*Источник (EN): [deleting-group-members.md](./deleting-group-members.md)*

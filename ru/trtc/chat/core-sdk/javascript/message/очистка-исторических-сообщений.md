# Очистка истории сообщений

## Обзор функции

Когда сообщения в беседе удаляются, все сообщения в беседе будут удалены как локально, так и из облака, но сама беседа не будет удалена.

> **Примечание:** Когда сообщения в беседе удаляются, `unreadCount` будет установлен в `0`, а содержимое `lastMessage` также будет очищено. Этот API не может быть использован для очистки сообщений в теме.

## Отображение в интерфейсе

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/bb7096d2cbe111ef97675254007c27c5.png)

## Очистка сообщений

##### **API**

```
chat.clearHistoryMessage(conversationID);
```

##### **Параметры**

| Имя | Тип | Описание |
| --- | --- | --- |
| conversationID | String | Идентификатор беседы в формате:`C2C${userID}` (для личных чатов)`GROUP{groupID}` (для групповых чатов) |

##### **Возвращаемое значение**

`Promise`

##### **Примеры**

```
// Очистить личные сообщения локально и из облакаlet promise = chat.clearHistoryMessage('C2CExample');promise.then(function(imResponse) {  // Сообщения успешно очищены}).catch(function(imError){  console.warn('clearHistoryMessage error:', imError); // Информация об ошибке очистки});
```

```
// Очистить групповые сообщения локально и из облакаlet promise = chat.clearHistoryMessage('GROUPExample');promise.then(function(imResponse) {  // Сообщения успешно очищены}).catch(function(imError){  console.warn('clearHistoryMessage error:', imError); // Информация об ошибке очистки});
```


---
*Источник: [https://trtc.io/document/53498](https://trtc.io/document/53498)*

---
*Источник (EN): [clear-history-message.md](очистка-исторических-сообщений.md)*

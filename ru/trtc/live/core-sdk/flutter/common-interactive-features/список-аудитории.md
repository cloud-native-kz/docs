# Список аудитории

Данная документация поможет вам использовать основной компонент AtomicXCore SDK, [LiveCoreWidget](https://tencent-rtc.github.io/TUIKit_Flutter/api_view_live_live_core_widget/api_view_live_live_core_widget-library.html), для быстрого создания базового приложения потокового видео с функциями трансляции хоста и просмотра аудитории.

## Основные возможности

**LiveCoreWidget** — это легкий виджет, созданный для сценариев потокового видео. В качестве основы реализации потокового видео он абстрагирует сложность потоковой передачи, совместного хостинга и рендеринга аудио и видео. Используйте LiveCoreWidget в качестве видеохолста для вашего потока, чтобы сосредоточиться на создании пользовательского интерфейса и интерактивных функций.

На изображении ниже показано, как LiveCoreWidget вписывается в интерфейс потокового видео:

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/989ce871f6d411f09fbf525400370dda.png)

## Подготовка

### Шаг 1: Активация сервиса

См. раздел [Активация сервиса](https://www.tencentcloud.com/document/product/647/60033) для получения пробной или платной версии SDK. Затем перейдите в [Консоль](https://console.trtc.io/app) для управления приложением и получите следующие данные:

- `SDKAppID`: идентификатор приложения (обязательно). TRTC использует `SDKAppID` для выставления счетов и деталей.
- `SDKSecretKey`: секретный ключ приложения.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/bb11696df6d411f09d0352540097cba1.png)

### Шаг 2: Импорт AtomicXCore в ваш проект

1. **Установка SDK**: добавьте зависимость `atomic_x_core` в файл `pubspec.yaml` и выполните `flutter pub get`.

```
dependencies:  atomic_x_core: ^3.6.0
```

2. **Настройка разрешений проекта:** требуется конфигурация как для проектов Android, так и iOS.

Android

iOS

Добавьте разрешения камеры и микрофона в файл `android/app/src/main/AndroidManifest.xml`.

```
<uses-permission android:name="android.permission.CAMERA" /><uses-permission android:name="android.permission.RECORD_AUDIO" />
```

Добавьте описания использования камеры и микрофона в **Podfile** вашего приложения в директории `iOS` и в файл `Info.plist` в директории `ios/Runner`.

**Podfile:**

```
post_install do |installer|  installer.pods_project.targets.each do |target|    flutter_additional_ios_build_settings(target)      target.build_configurations.each do |config|          config.build_settings['GCC_PREPROCESSOR_DEFINITIONS'] ||= [        '$(inherited)',        'PERMISSION_MICROPHONE=1',        'PERMISSION_CAMERA=1',        ]      end  endend
```

**Info.plist:**

```
<key>NSCameraUsageDescription</key><string>TUILiveKit needs to access your camera permission. Video recording with picture only after enabling.</string><key>NSMicrophoneUsageDescription</key><string>TUILiveKit needs to access your mic permission. Recorded video will have sound when enabled.</string>
```

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/548e6c77f82711f093ec525400074c32.png)

### Шаг 3: Реализация логики входа

Вызовите `LoginStore.shared.login` в вашем проекте для входа. **Это ключевой этап использования всех функций AtomicXCore.**

> **Важно:** рекомендуется вызвать `LoginStore.shared.login` после успешного входа в учетную запись пользователя вашего приложения для обеспечения четкой и согласованной бизнес-логики.

```
import 'package:flutter/material.dart';import 'package:atomic_x_core/atomicxcore.dart';void main() async {  WidgetsFlutterBinding.ensureInitialized();  final result = await LoginStore.shared.login(    sdkAppID: 1400000001,           // replace with your sdkAppID    userID: "test_001",             // replace with your userID    userSig: "xxxxxxxxxxx",         // replace with your userSig  );  if (result.isSuccess) {    debugPrint("login success");  } else {    debugPrint("login failed code: ${result.errorCode}, message: ${result.errorMessage}");  }  runApp(const MyApp());}
```

**Параметры API входа:**

| **Параметр** | **Тип** | **Примечание** |
| --- | --- | --- |
| `sdkAppID` | `int` | Идентификатор вашего приложения, который можно получить из [консоли TRTC](https://console.trtc.io/app). |
| `userID` | `String` | Уникальный идентификатор текущего пользователя. Разрешены только буквы, цифры, дефисы и подчеркивания. Чтобы избежать конфликтов входа на разных устройствах, не используйте простые ID, такие как 1, 123 и т. д. |
| `userSig` | `String` | Учетные данные для аутентификации TRTC. Пожалуйста, обратите внимание: в среде разработки вы можете использовать локальную функцию `GenerateTestUserSig.genTestSig` для генерации `UserSig` или сгенерировать временный UserSig через [инструмент генерации UserSig](https://console.trtc.io/usersig). В рабочей среде для предотвращения утечки SecretKey всегда генерируйте `UserSig` на вашем сервере. Для получения подробной информации см. раздел [Генерация UserSig на сервере](https://www.tencentcloud.com/document/product/647/35166). |

## Создание базовой комнаты трансляции

### Шаг 1: Настройка видеотрансляции хоста

Выполните следующие шаги для быстрой настройки видеотрансляции хоста:

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/332cea8bf6d711f09fbf525400370dda.png)

> **Примечание:** для полной реализации трансляции хоста см. файл [live_room_anchor_widget.dart](https://github.com/Tencent-RTC/TUIKit_Flutter/blob/main/live/livekit/lib/live_stream/features/live_room_anchor_widget.dart) в открытом проекте TUILiveKit.

1. **Инициализация представления трансляции хоста**

На странице хоста создайте экземпляр `LiveCoreWidget` и управляйте поведением потока через `LiveCoreController`.

```
import 'package:flutter/material.dart';import 'package:atomic_x_core/atomicxcore.dart';/// YourAnchorPage represents your anchor starts live streaming pageclass YourAnchorPage extends StatefulWidget {  final String liveId;  const YourAnchorPage({super.key, required this.liveId});  @override  State<YourAnchorPage> createState() => _YourAnchorPageState();}class _YourAnchorPageState extends State<YourAnchorPage> {  // 1. Create a LiveCoreController instance  late final LiveCoreController _controller;  @override  void initState() {    super.initState();    // 2. Initialize the controller    _controller = LiveCoreController.create();    // 3. Set up live streaming ID    _controller.setLiveID(widget.liveId);  }  @override  Widget build(BuildContext context) {    return Scaffold(      body: Stack(        children: [          // 4. Load the broadcaster stream page to your view          LiveCoreWidget(controller: _controller),          // Your other UI components        ],      ),    );  }}
```

2. **Включение камеры и микрофона**

Вызовите `DeviceStore.shared.openLocalCamera` и `DeviceStore.shared.openLocalMicrophone` для включения камеры и микрофона. LiveCoreWidget будет автоматически предпросмотром видеопотока камеры:

```
import 'package:flutter/material.dart';import 'package:atomic_x_core/atomicxcore.dart';/// YourAnchorPage represents your anchor starts live streaming pageclass YourAnchorPage extends StatefulWidget {  final String liveId;  const YourAnchorPage({super.key, required this.liveId});  @override  State<YourAnchorPage> createState() => _YourAnchorPageState();}class _YourAnchorPageState extends State<YourAnchorPage> {  late final LiveCoreController _controller;  @override  void initState() {    super.initState();    _controller = LiveCoreController.create();    _controller.setLiveID(widget.liveId);    // Turn on the device    _openDevices();  }  void _openDevices() {    // 1. Open front camera    DeviceStore.shared.openLocalCamera(true);    // 2. Turn on the microphone    DeviceStore.shared.openLocalMicrophone();  }  @override  Widget build(BuildContext context) {    return Scaffold(      body: Stack(        children: [          LiveCoreWidget(controller: _controller),        ],      ),    );  }}
```

3. **Начало трансляции**

Вызовите API **LiveListStore** `createLive` для начала видеотрансляции:

```
import 'package:flutter/material.dart';import 'package:atomic_x_core/atomicxcore.dart';/// YourAnchorPage represents your anchor starts live streaming pageclass YourAnchorPage extends StatefulWidget {  final String liveId;  const YourAnchorPage({super.key, required this.liveId});  @override  State<YourAnchorPage> createState() => _YourAnchorPageState();}class _YourAnchorPageState extends State<YourAnchorPage> {  late final LiveCoreController _controller;  @override  void initState() {    super.initState();    _controller = LiveCoreController.create();    _controller.setLiveID(widget.liveId);    _openDevices();  }  void _openDevices() {    DeviceStore.shared.openLocalCamera(true);    DeviceStore.shared.openLocalMicrophone();  }  // Call the go live API to start live streaming  Future<void> _startLive() async {    // 1. Prepare the LiveInfo object    final liveInfo = LiveInfo(      // Set up live streaming room id      liveID: widget.liveId,      // Set up live streaming room name      liveName: "test live stream",      // Configure the layout template, default: 600 dynamic grid layout      seatLayoutTemplateID: 600,      // Configure the anchor to always stay on the seat      keepOwnerOnSeat: true,    );    // 2. Call LiveListStore.shared.createLive to start live streaming    final result = await LiveListStore.shared.createLive(liveInfo);    if (result.isSuccess) {      debugPrint("startLive success");    } else {      debugPrint("startLive error: ${result.errorMessage}");    }  }  @override  Widget build(BuildContext context) {    return Scaffold(      body: Stack(        children: [          LiveCoreWidget(controller: _controller),          // Go live button          Positioned(            bottom: 50,            left: 0,            right: 0,            child: Center(              child: ElevatedButton(                onPressed: _startLive,                child: const Text('Start live broadcast'),              ),            ),          ),        ],      ),    );  }}
```

**Параметры LiveInfo:**

| **Имя параметра** | **Тип** | **Атрибут** | **Описание** |
| --- | --- | --- | --- |
| `liveID` | `String` | Обязательно | Уникальный идентификатор комнаты потоковой трансляции. |
| `liveName` | `String` | Опционально | Имя комнаты потоковой трансляции. |
| `notice` | `String` | Опционально | Информация об объявлении в комнате потоковой трансляции. |
| `isMessageDisable` | `bool` | Опционально | Отключить звук (`true`: да, `false`: нет). |
| `isPublicVisible` | `bool` | Опционально | Является ли комната трансляции общедоступной (`true`: да, `false`: нет). |
| `isSeatEnabled` | `bool` | Опционально | Включить функцию мест (`true`: включено, `false`: отключено). |
| `keepOwnerOnSeat` | `bool` | Опционально | Остается ли владелец комнаты на месте. |
| `maxSeatCount` | `int` | Опционально | Максимальное количество мест. |
| `seatMode` | `TakeSeatMode` | Опционально | Режим мест: `TakeSeatMode.free`: свободное место, `TakeSeatMode.apply`: применить для места |
| `seatLayoutTemplateID` | `int` | Обязательно | Идентификатор шаблона макета места. |
| `coverURL` | `String` | Опционально | URL изображения обложки комнаты потоковой трансляции. |
| `backgroundURL` | `String` | Опционально | URL изображения фона комнаты потоковой трансляции. |
| `categoryList` | `List<int>` | Опционально | Теги категорий для комнаты трансляции. |
| `activityStatus` | `int` | Опционально | Статус потока. |
| `isGiftEnabled` | `bool` | Опционально | Включить ли функцию подарков (`true`: да, `false`: нет). |

4. **Завершение трансляции**

При завершении трансляции вызовите `LiveListStore.shared.endLive` для остановки потока и закрытия комнаты. SDK будет автоматически обрабатывать очистку.

```
import 'package:flutter/material.dart';import 'package:atomic_x_core/atomicxcore.dart';/// YourAnchorPage represents your anchor starts live streaming pageclass YourAnchorPage extends StatefulWidget {  final String liveId;  const YourAnchorPage({super.key, required this.liveId});  @override  State<YourAnchorPage> createState() => _YourAnchorPageState();}class _YourAnchorPageState extends State<YourAnchorPage> {  // ... Other code ...  End live streaming  Future<void> _stopLive() async {    final result = await LiveListStore.shared.endLive();    if (result.isSuccess) {      debugPrint("endLive success");    } else {      debugPrint("endLive error: ${result.errorMessage}");    }  }  // ... Other code ...}
```

### Шаг 2: Вход в комнату аудитории и просмотр

Включите просмотр трансляции для аудитории, выполнив следующие шаги:

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/92e9b016f6d711f0b34b525400ecee81.png)

> **Примечание:** для полной реализации аудитории см. файл [audience_widget.dart](https://github.com/Tencent-RTC/TUIKit_Flutter/blob/main/live/livekit/lib/live_stream/features/audience/audience_widget.dart) в открытом проекте TUILiveKit.

1. **Страница потоковой трансляции аудитории**

На странице аудитории создайте экземпляр `LiveCoreWidget` и управляйте поведением потока через `LiveCoreController`.

```
import 'package:flutter/material.dart';import 'package:atomic_x_core/atomicxcore.dart';/// YourAudiencePage represents your audience viewing webpageclass YourAudiencePage extends StatefulWidget {  final String liveId;  const YourAudiencePage({super.key, required this.liveId});  @override  State<YourAudiencePage> createState() => _YourAudiencePageState();}class _YourAudiencePageState extends State<YourAudiencePage> {  // 1. Create a LiveCoreController instance  late final LiveCoreController _controller;  @override  void initState() {    super.initState();    // 2. Initialize the controller    _controller = LiveCoreController.create();    // 3. Bind live streaming id    _controller.setLiveID(widget.liveId);  }  @override  Widget build(BuildContext context) {    return Scaffold(      body: Stack(        children: [          // 4. Load the audience pull stream page to your view          LiveCoreWidget(controller: _controller),        ],      ),    );  }}
```

2. **Присоединение к комнате трансляции**

Вызовите `LiveListStore.shared.joinLive` для присоединения к потоку. LiveCoreWidget будет автоматически воспроизводить видеопоток:

```
import 'package:flutter/material.dart';import 'package:atomic_x_core/atomicxcore.dart';/// YourAudiencePage represents your audience viewing webpageclass YourAudiencePage extends StatefulWidget {  final String liveId;  const YourAudiencePage({super.key, required this.liveId});  @override  State<YourAudiencePage> createState() => _YourAudiencePageState();}class _YourAudiencePageState extends State<YourAudiencePage> {  late final LiveCoreController _controller;  @override  void initState() {    super.initState();    _controller = LiveCoreController.create();    _controller.setLiveID(widget.liveId);    // Enter live room    _joinLive();  }  Future<void> _joinLive() async {    // Call LiveListStore.shared.joinLive to enter live room    // - liveId: same liveId as anchor starts live streaming    final result = await LiveListStore.shared.joinLive(widget.liveId);    if (result.isSuccess) {      debugPrint("joinLive success");    } else {      debugPrint("joinLive error: ${result.errorMessage}");      // Failed to enter the room, must exit the page      // if (mounted) Navigator.of(context).pop();    }  }  @override  Widget build(BuildContext context) {    return Scaffold(      body: Stack(        children: [          LiveCoreWidget(controller: _controller),        ],      ),    );  }}
```

3. **Выход из комнаты трансляции**

Когда аудитория уходит, вызовите `LiveListStore.shared.leaveLive` для выхода. SDK будет автоматически остановить поток и выйти из комнаты.

```
import 'package:flutter/material.dart';import 'package:atomic_x_core/atomicxcore.dart';/// YourAudiencePage represents your audience viewing webpageclass YourAudiencePage extends StatefulWidget {  final String liveId;  const YourAudiencePage({super.key, required this.liveId});  @override  State<YourAudiencePage> createState() => _YourAudiencePageState();}class _YourAudiencePageState extends State<YourAudiencePage> {  // ... Other code ...  End live stream  Future<void> _leaveLive() async {    final result = await LiveListStore.shared.leaveLive();    if (result.isSuccess) {      debugPrint("leaveLive success");    } else {      debugPrint("leaveLive error: ${result.errorMessage}");    }  }  // ... Other code ...}
```

### Шаг 3: Прослушивание событий трансляции

После того как аудитория присоединится к комнате трансляции, вы должны обработать пассивные события, такие как завершение трансляции хостом или исключение аудитории. Без обработки событий пользовательский интерфейс аудитории может остаться на черном экране, что повлияет на пользовательский опыт.

Используйте `LiveListListener` для прослушивания событий трансляции:

```
import 'package:flutter/material.dart';import 'package:atomic_x_core/atomicxcore.dart';/// YourAudiencePage represents your audience viewing webpageclass YourAudiencePage extends StatefulWidget {  final String liveId;  const YourAudiencePage({super.key, required this.liveId});  @override  State<YourAudiencePage> createState() => _YourAudiencePageState();}class _YourAudiencePageState extends State<YourAudiencePage> {  late final LiveCoreController _controller;  // 1. Define LiveListListener to manage event monitoring  late final LiveListListener _liveListListener;  @override  void initState() {    super.initState();    _controller = LiveCoreController.create();    _controller.setLiveID(widget.liveId);    // 2. Listen to Live Event    _setupLiveEventListener();    // 3. Enter live room    _joinLive();  }  // 4. Add a method to set up event listening  void _setupLiveEventListener() {    _liveListListener = LiveListListener(      onLiveEnded: (liveID, reason, message) {        // Listen to live streaming end        debugPrint("Live ended. liveID: $liveID, reason: ${reason.value}, message: $message");        // Handle the logic for exiting a live streaming room herein, such as closing current page        // if (mounted) Navigator.of(context).pop();      },      onKickedOutOfLive: (liveID, reason, message) {        // Listen to being kicked out of live stream        debugPrint("Kicked out of live. liveID: $liveID, reason: ${reason.value}, message: $message");        // Handle the logic for exiting a live streaming room herein        // if (mounted) Navigator.of(context).pop();      },    );    LiveListStore.shared.addLiveListListener(_liveListListener);  }  Future<void> _joinLive() async {    final result = await LiveListStore.shared.joinLive(widget.liveId);    if (result.isSuccess) {      debugPrint("joinLive success");    } else {      debugPrint("joinLive error: ${result.errorMessage}");    }  }  Future<void> _leaveLive() async {    final result = await LiveListStore.shared.leaveLive();    if (result.isSuccess) {      debugPrint("leaveLive success");    } else {      debugPrint("leaveLive error: ${result.errorMessage}");    }  }  @override  void dispose() {    // 5. Remove event listening    LiveListStore.shared.removeLiveListListener(_liveListListener);    super.dispose();  }  @override  Widget build(BuildContext context) {    return Scaffold(      body: Stack(        children: [          LiveCoreWidget(controller: _controller),        ],      ),    );  }}
```

### Запуск и тестирование

После интеграции LiveCoreWidget у вас будет чистое представление видеорендеринга со всеми возможностями потоковой трансляции, но без интерактивного пользовательского интерфейса. См. следующий раздел [Расширение сценариев потоковой трансляции](#расширение-сценариев-потоковой-трансляции), чтобы добавить больше функций в вашу трансляцию.

| Макет | **Динамический макет сетки** | **Макет плавающего окна** | **Фиксированный макет сетки** |
| --- | --- | --- | --- |
| Идентификатор шаблона | 600 | 601 | 800 |
| Описание | Макет по умолчанию; размер сетки динамически корректируется в зависимости от количества со-хостов. | Со-хосты отображаются как плавающие окна. | Фиксированное количество со-хостов; каждый занимает фиксированную сетку. |
| Пример | ![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/6a730a2ecc0d11f084a45254005ef0f7.png) | ![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/71c322e9cc0d11f084a45254005ef0f7.png) | ![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/a1855792cc0d11f0ae4f52540099c741.png) |

## Расширение сценариев потоковой трансляции

После внедрения базовой функциональности потоковой трансляции обратитесь к следующим руководствам, чтобы добавить интерактивные функции в вашу трансляцию:

| **Функция** | **Описание** | **Хранилище** | **Руководство по реализации** |
| --- | --- | --- | --- |
| **Совместное видеотрансляция аудитории** | Аудитория может подать заявку на присоединение к хосту и взаимодействовать через видео в реальном времени. | [CoGuestStore](https://tencent-rtc.github.io/TUIKit_Flutter/api_live_co_guest_store/CoGuestStore-class.html) | [Руководство по реализации](https://www.tencentcloud.com/document/product/647/77553) |
| **Cross-room PK хостов** | Хосты из разных комнат могут подключиться для взаимодействия или PK. | [CoHostStore](https://tencent-rtc.github.io/TUIKit_Flutter/api_live_co_host_store/CoHostStore-class.html) / [BattleStore](https://tencent-rtc.github.io/TUIKit_Flutter/api_live_battle_store/BattleStore-class.html) | [Руководство по реализации](https://www.tencentcloud.com/document/product/647/77554) |
| **Чат с комментариями потока** | Аудитория может отправлять и получать текстовые сообщения в реальном времени в комнате трансляции. | [BarrageStore](https://tencent-rtc.github.io/TUIKit_Flutter/api_barrage_barrage_store/BarrageStore-class.html) | [Руководство по реализации](https://www.tencentcloud.com/document/product/647/77555) |
| **Система подарков** | Аудитория может отправлять виртуальные подарки хосту, увеличивая вовлеченность и веселье. | [GiftStore](https://tencent-rtc.github.io/TUIKit_Flutter/api_gift_gift_store/GiftStore-class.html) | [Руководство по реализации](https://www.tencentcloud.com/document/product/647/77556) |

## Документация API

| **Хранилище/Компонент** | Описание функции | Документация API |
| --- | --- | --- |
| **LiveCoreWidget** | Основное представление для отображения и взаимодействия видео в прямом эфире: обрабатывает рендеринг видео и управление виджетами; поддерживает трансляцию хоста, совместное видеотрансляция аудитории, cross-room соединения хостов и т. д. | [Документация API](https://tencent-rtc.github.io/TUIKit_Flutter/api_view_live_live_core_widget/LiveCoreWidget-class.html) |
| **LiveCoreController** | Контроллер для LiveCoreWidget: установка идентификатора потока, управление предпросмотром и другие операции. | [Документация API](https://tencent-rtc.github.io/TUIKit_Flutter/api_view_live_live_core_widget/LiveCoreController-class.html) |
| **LiveListStore** | Управление жизненным циклом комнаты трансляции: создание, присоединение, выход, уничтожение комнаты; запрос списка комнат; обновление информации о трансляции (имя, объявление и т. д.); прослушивание статуса трансляции (исключение, завершение). | [Документация API](https://tencent-rtc.github.io/TUIKit_Flutter/api_live_live_list_store/LiveListStore-class.html) |
| **DeviceStore** | Управление устройствами аудио/видео: микрофон (переключение/громкость), камера (переключение/смена камеры/качество видео), совместное использование экрана, мониторинг статуса устройства в реальном времени. | [Документация API](https://tencent-rtc.github.io/TUIKit_Flutter/api_device_device_store/DeviceStore-class.html) |
| **CoGuestStore** | Управление совместными видеотрансляциями аудитории: заявки на совместную видеотрансляцию/приглашения/принять/отклонить, разрешения членов (микрофон/камера), синхронизация статуса. | [Документация API](https://tencent-rtc.github.io/TUIKit_Flutter/api_live_co_guest_store/CoGuestStore-class.html) |
| **CoHostStore** | Cross-room соединение хостов: поддерживает нес

---
*Источник (EN): [audience-list.md](список-аудитории.md)*

# Список комнат

## Обзор функции

Этот документ представляет собой полное введение в **страницу Live List голосовой чат-комнаты** в `TUILiveKit`. Используйте это руководство для быстрой интеграции готовой страницы списка в ваш проект или для глубокой кастомизации стиля, макета и функций страницы в соответствии с вашими требованиями.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/d08b5250c07111f08c0e52540044a08e.png)

## Быстрая интеграция

### Интегрируйте компонент

Следуйте [Предварительным требованиям](https://www.tencentcloud.com/document/product/647/60335) для интеграции `TUILiveKit` в ваш проект.

### Добавьте LiveListView

Сценарий голосовой чат-комнаты в настоящее время поддерживает только макет каскада из двух столбцов.

```
import android.content.Contextimport android.os.Bundleimport androidx.appcompat.app.AppCompatActivityimport com.trtc.uikit.livekit.features.livelist.LiveListViewimport com.trtc.uikit.livekit.features.livelist.Styleclass YourActivity : AppCompatActivity() {    override fun onCreate(savedInstanceState: Bundle?) {        super.onCreate(savedInstanceState)        // 1. Create and initialize the waterfall view        val view = createLiveListView(this)        // 2. Add the LiveListView to your Activity or Fragment        setContentView(view)    }    fun createLiveListView(context: Context): LiveListView {        val liveListView = LiveListView(context)        // Initialize with a double-column waterfall layout        liveListView.init(this, Style.DOUBLE_COLUMN)        return liveListView    }}
```

### Включите навигацию со списка Live на страницу просмотра аудитории

Список Live обрабатывает события клика через обратный вызов `OnItemClickListener`. Реализуйте OnItemClickListener в представлении списка Live для обработки кликов пользователя и перейдите на страницу просмотра аудитории в `onItemClick`. Подробнее о реализации страницы просмотра аудитории см. в разделе [Просмотр аудитории](https://www.tencentcloud.com/document/product/647/74327).

**Пример взаимодействия:**

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/dca807cbc07111f08c0e52540044a08e.png)

**Пример кода:**

```
fun createLiveListView(context: Context): LiveListView {    val liveListView = LiveListView(context)    // Initialize with a double-column waterfall layout    liveListView.init(this, Style.DOUBLE_COLUMN)    liveListView.setOnItemClickListener { view, liveInfo ->        // Navigate to the audience watch page when a list item is clicked        val intent = Intent(context, YourAudienceActivity::class.java)        intent.putExtra("liveId", liveInfo.roomId)        context.startActivity(intent)    }    return liveListView}
```

## Кастомизируйте UI

`TUILiveKit` предоставляет гибкие API для кастомизации компонента каскада списка Live. Вы можете кастомизировать источник данных и внешний вид элементов списка в соответствии с требованиями вашего бизнеса.

### Пользовательский источник данных

Если ваш бэкенд предоставляет пользовательский источник данных списка Live, реализуйте интерфейс `LiveListDataSource` для предоставления собственных данных вместо использования данных компонента по умолчанию.

```
// 1. Import dependenciesimport com.trtc.uikit.livekit.features.livelist.LiveListDataSource;// 2. Implement LiveListDataSource to provide a custom data sourceval dataSource = object : LiveListDataSource {    override fun fetchLiveList(param: FetchLiveListParam, callback: LiveListCallback) {        // Connect to your backend and return data in the required format        val liveInfoList = mutableListOf<TUILiveListManager.LiveInfo>()        val liveInfo = TUILiveListManager.LiveInfo().apply {            roomInfo = TUIRoomDefine.RoomInfo().apply {                roomId = "live_123456"                name = "live_123456"            }        }        liveInfoList.add(liveInfo)        val cursor = "aabbccdd"        callback.onSuccess(cursor, liveInfoList)    }}// 3. Pass the custom dataSource during initializationliveListView.init(this, Style.DOUBLE_COLUMN, dataSource = dataSource)
```

### Пользовательское представление

По умолчанию элементы списка каскада отображают обложку комнаты. Для кастомизации элементов UI в верхней части элементов списка (таких как аватар хоста, название трансляции и т.д.), реализуйте интерфейс `LiveListViewAdapter`.

```
// 1. Import dependenciesimport com.trtc.uikit.livekit.features.livelist.LiveListViewAdapter;// 2. Implement LiveListViewAdapter to customize widgetsval liveListViewAdapter = object : LiveListViewAdapter {    override fun createLiveInfoView(liveInfo: TUILiveListManager.LiveInfo): View {        // Create your custom widget view        val widgetView = YourView(context)        widgetView.init(liveInfo)        return widgetView    }    override fun updateLiveInfoView(view: View, liveInfo: TUILiveListManager.LiveInfo) {        // Update the widget view with new data        val widgetView = view as YourView        widgetView.updateLiveInfoView(liveInfo)    }}// 3. Pass the custom liveListViewAdapter during initializationliveListView.init(this, Style.DOUBLE_COLUMN, adapter = liveListViewAdapter)
```

## Следующие шаги

Вы успешно интегрировали функцию **Список Live трансляций**. Далее реализуйте дополнительные функции, такие как **Трансляция хостом** и **Просмотр аудиторией**. Подробнее см. в таблице ниже:

| **Функция** | **Описание** | **Руководство интеграции** |
| --- | --- | --- |
| **Трансляция хостом** | Реализует полный процесс запуска хостом голосовой чат-комнаты в прямом эфире, включая предварительную настройку и взаимодействие во время сеанса. | [Руководство интеграции](https://www.tencentcloud.com/document/product/647/74323) |
| **Просмотр аудиторией** | Включает участие аудитории после присоединения к голосовой чат-комнате хоста, включая включение микрофона, отправку и получение комментариев, и многое другое. | [Руководство интеграции](https://www.tencentcloud.com/document/product/647/74327) |

## Часто задаваемые вопросы

### Почему после интеграции функции списка Live трансляции не отображаются?

Если страница пустая, убедитесь, что вы завершили [вход](https://www.tencentcloud.com/document/product/647/60335#CompleteLogin). Для тестирования используйте два устройства: начните трансляцию на одном устройстве и откройте страницу списка Live на другом. Список Live отобразит все активные комнаты.

### Что делать, если я использую пользовательский источник данных (LiveListDataSource), но список не отображается или не обновляется?

Убедитесь, что вы правильно реализовали интерфейс `LiveListDataSource`. Обратите особое внимание на следующее:

1. Подтвердите, что вызывается метод `fetchLiveList`.
2. Всегда вызывайте `callback.onSuccess` или `callback.onFailure` после получения данных, независимо от результата.
3. Проверьте, что ваш бэкенд возвращает данные в правильном формате.


---
*Источник: [https://trtc.io/document/74325](https://trtc.io/document/74325)*

---
*Источник (EN): [room-list.md](список-комнат.md)*

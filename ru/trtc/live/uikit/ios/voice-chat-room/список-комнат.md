# Список комнат

## Обзор функции

Этот документ содержит полное введение в **страницу живого списка голосовых комнат чата** в `TUILiveKit`. Используйте это руководство для быстрой интеграции встроенной страницы списка в ваш проект или для глубокой настройки стиля, макета и функций страницы в соответствии с вашими требованиями.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/8e43870ec08311f0b35752540099c741.png)

## Быстрая интеграция

### Интеграция компонента

Следуйте [Предварительным требованиям](https://www.tencentcloud.com/document/product/647/60334) для интеграции `TUILiveKit` в ваш проект.

### Добавление LiveListView

Сценарий голосовой чат-комнаты в настоящее время поддерживает только двухколонный макет водопада.

```
// Example: YourLiveListViewController represents your live list waterfall view controllerclass YourLiveListViewController: UIViewController {    // 1. Declare liveListView as a member variable    private let liveListView = LiveListView(style: .doubleColumn)     public override func viewDidLoad() {        super.viewDidLoad()        // 2. Add liveListView to the view        view.addSubview(liveListView)        liveListView.snp.makeConstraints { make in            make.edges.equalToSuperview()        }        // 3. Set the item click delegate for the list        liveListView.itemClickDelegate = self    }}
```

### Реализация навигации со страницы списка трансляций на страницу просмотра аудиторией

Список трансляций обрабатывает события щелчка по элементам с помощью обратного вызова `OnItemClickListener`. Реализуйте `OnItemClickListener` в контроллере представления водопада списка трансляций для обработки щелчков пользователя и навигации на страницу просмотра аудиторией в `onItemClick`. Подробнее о реализации страницы просмотра аудиторией см. [Просмотр аудиторией](https://www.tencentcloud.com/document/product/647/74328).

**Пример взаимодействия:**

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/e52657cfc07111f083155254001c06ec.png)

**Пример кода:**

```
extension YourLiveListViewController: OnItemClickDelegate {    func onItemClick(liveInfo: LiveInfo, frame: CGRect) {        // 1. Instantiate your audience watch view controller        let audienceVC = YourAudienceViewController(roomId: liveInfo.roomId)        audienceVC.modalPresentationStyle = .fullScreen        // 2. Present the audience watch view controller        present(audienceVC, animated: false)    }}
```

## Настройка пользовательского интерфейса

`TUILiveKit` предоставляет гибкие API для настройки компонента водопада списка трансляций. Вы можете настроить источник данных и внешний вид элементов списка в соответствии с потребностями вашего бизнеса.

### Пользовательский источник данных

Если ваш бэкенд предоставляет пользовательский список трансляций, реализуйте интерфейс `LiveListDataSource`, чтобы предоставить свои собственные данные вместо использования данных по умолчанию компонента.

```
// Example: YourLiveListViewController represents your live list waterfall view controllerclass YourLiveListViewController: UIViewController {    private let liveListView: LiveListView = LiveListView(style: .doubleColumn)     public override func viewDidLoad() {        super.viewDidLoad()        view.addSubview(liveListView)        liveListView.snp.makeConstraints { make in            make.edges.equalToSuperview()        }        liveListView.itemClickDelegate = self        // 1. Set the custom data source delegate        liveListView.dataSource = self    }}// 2. Implement the custom data source delegate: LiveListDataSourceextension YourLiveListViewController: LiveListDataSource {    public func fetchLiveList(cursor: String, onSuccess: @escaping LiveListBlock, onError: @escaping TUIErrorBlock) {        // 3. Connect to your backend and return data to the UI component in the following format        var liveInfoList: [LiveInfo] = []        var liveInfo = LiveInfo()        liveInfo.roomId = "live_123456"        liveInfo.name = "live_123456"        liveInfoList.append(liveInfo)        let cursor = "aabbccdd"        onSuccess(cursor, liveInfoList)    }}
```

### Пользовательское представление

По умолчанию элемент списка водопада отображает обложку комнаты. Для настройки верхних элементов пользовательского интерфейса элемента списка (таких как аватар ведущего, название трансляции и т. д.) реализуйте интерфейс `LiveListViewAdapter`.

```
// Example: YourLiveListViewController represents your live list waterfall view controllerclass YourLiveListViewController: UIViewController {    private let liveListView: LiveListView = LiveListView(style: .doubleColumn)     public override func viewDidLoad() {        super.viewDidLoad()        view.addSubview(liveListView)        liveListView.snp.makeConstraints { make in            make.edges.equalToSuperview()        }        liveListView.itemClickDelegate = self        liveListView.dataSource = self        // 1. Set the custom widget adapter        liveListView.adapter = self    }}// 2. Implement the custom widget adapterextension YourLiveListViewController: LiveListViewAdapter {    public func createLiveInfoView(info: LiveInfo) -> UIView {        // Custom widget view        return YourCustomView(liveInfo: info)    }        public func updateLiveInfoView(view: UIView, info: LiveInfo) {        if let infoView = view as? YourCustomView {            // Update the data bound to the widget view            infoView.updateView(liveInfo: info)        }     }}
```

## Следующие шаги

Вы успешно интегрировали функцию списка живых трансляций. Далее реализуйте дополнительные функции, такие как **Трансляция ведущего** и **Просмотр аудиторией**. Подробности см. в таблице ниже:

| **Функция** | **Описание** | **Руководство по интеграции** |
| --- | --- | --- |
| **Трансляция ведущего** | Реализует полный процесс начала ведущим прямой трансляции голосовой чат-комнаты, включая подготовку перед трансляцией и взаимодействия во время сеанса. | [Руководство по интеграции](https://www.tencentcloud.com/document/product/647/74324) |
| **Просмотр аудиторией** | Обеспечивает участие аудитории после присоединения к голосовой чат-комнате ведущего, включая присоединение к микрофону, отправку и получение комментариев и многое другое. | [Руководство по интеграции](https://www.tencentcloud.com/document/product/647/74328) |

## Часто задаваемые вопросы

### Почему после интеграции функции списка трансляций не отображаются живые трансляции?

Если страница пуста, проверьте, что вы завершили [вход](https://www.tencentcloud.com/document/product/647/60334#6ebcab01-c3d7-4fb4-a933-016cf1cf5193). Для тестирования используйте два устройства: запустите прямую трансляцию на одном устройстве и откройте страницу списка трансляций на другом устройстве. Список трансляций будет отображать любые активные комнаты.

### Что мне делать, если я использую пользовательский источник данных (LiveListDataSource), но список не отображается или не обновляется?

Убедитесь, что вы правильно реализовали интерфейс `LiveListDataSource`. Обратите особое внимание на следующее:

1. Подтвердите, что вызывается метод `fetchLiveList`.
2. Всегда вызывайте `callback.onSuccess` или `callback.onFailure` после получения данных, независимо от результата.
3. Проверьте, что ваш бэкенд возвращает данные в правильном формате.


---
*Источник: [https://trtc.io/document/74326](https://trtc.io/document/74326)*

---
*Источник (EN): [room-list.md](список-комнат.md)*

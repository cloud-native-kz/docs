# Интеграция

В этом руководстве в основном описано, как реализовать базовый аудио- и видеозвонок.

## Предусловия

- ОС: Windows 7 или более поздняя версия.
- Среда разработки: Visual Studio 2019 или более поздняя версия (рекомендуется v2022).

## Руководство по интеграции

### Шаг 1. Импорт TRTC SDK

1. Откройте **Visual Studio** и создайте собственное приложение **MFC**. На странице мастера **MFC Application** выберите **Dialog based** для типа приложения **Application type** и используйте параметры по умолчанию для других конфигураций мастера. Затем нажмите **Finish**.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/87149e427ca311ef852f52540075b605.png)

2. Загрузите [Windows SDK](https://liteav.sdk.qcloud.com/download/latest/v1/TXLiteAVSDK_TRTC_Win_latest.zip) и скопируйте распакованные файлы **SDK** в каталог, где находится файл `.vcxproj`.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/870abeb57ca311efa87a525400bdab9d.png)

### Шаг 2. Конфигурация проекта

Дважды щелкните файл проекта `.sln`, затем в **Solution Explorer** в правой панели инструментов > **Правая кнопка мыши** для проекта > **Properties** и выполните следующие конфигурации:

1. **Добавить дополнительные каталоги включения:**

Следуя пути **C/C++** > **General** > **Additional Include Directories**, добавьте **каталог заголовков SDK:** `$(ProjectDir)SDK\\CPlusPlus\\Win64\\include` и `$(ProjectDir)SDK\\CPlusPlus\\Win64\\include\\TRTC`.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/871181d77ca311ef8631525400a9236a.png)

2. **Добавить дополнительные каталоги библиотек:**

Следуя пути **Linker** > **General** > **Additional Library Directories**, добавьте **каталог библиотек SDK**: `$(ProjectDir)SDK\\CPlusPlus\\Win64\\lib`.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/870681f47ca311ef80ff525400d5f8ef.png)

3. **Добавить дополнительные зависимости:**

Следуя пути **Linker** > **Input** > **Additional Dependencie**, добавьте **файлы библиотек SDK**: `liteav.lib`.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/871c09a57ca311ef8829525400fdb830.png)

4. **Добавить командную строку:**

Следуя пути **Build Events** > **Post-Build Event** > **Command line**, добавьте `copy /Y $(ProjectDir)SDK\\CPlusPlus\\Win64\\lib\\*.dll  $(OutDir)`.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/8724850d7ca311ef8631525400a9236a.png)

5. **Вывести версию SDK:**

Дважды щелкните файл **.cpp** в **Solution Explorer** и импортируйте файл заголовка **TRTC**:

```
#include "ITRTCCloud.h"
```

> **Примечание:** Ссылайтесь на **"ITRTCCloud.h"** после существующего файла заголовка, иначе вы получите ошибку `ITRTCCloud: undefined identifier`.

Добавьте следующий код в функцию `OnInitDialog`:

```
ITRTCCloud * pTRTCCloud = getTRTCShareInstance();CString szText;szText.Format(L"SDK version: %hs", pTRTCCloud->getSDKVersion());CWnd *pStatic = GetDlgItem(IDC_STATIC);pStatic->SetWindowTextW(szText);
```

После завершения описанных выше шагов нажмите **Run** для вывода номера версии SDK.

![](https://cloudcache.intl.tencent-cloud.com/cms/backend-cms/871748d97ca311ef82535254002693fd.png)

> **Примечание:** Если вы получите сообщение об ошибке **"module machine type 'x86' conflicts with target machine type 'x64' "**, выберите **'x64'** в платформе решения.

### Шаг 3. Создание экземпляра TRTC

1. Ссылайтесь на файл заголовка `"ITRTCCloud.h"` в файле **'PROJECTNAME'.h**, сделайте класс в этом файле публично наследующимся от **CWinApp** и **ITRTCCloudCallback**, и объявите переменные-члены событий прослушивания и **TRTCCloud**.

```
#include "ITRTCCloud.h" // Include the TRTC header fileclass CRTCWindowsApp : public CWinApp, public ITRTCCloudCallback{public:	CRTCWindowsApp();// Overridespublic:	virtual BOOL InitInstance();	virtual void onError(TXLiteAVError errCode, const char* errMsg, void* extraInfo) override; // Listen for the 'onError' event	virtual void onWarning(TXLiteAVWarning warningCode, const char* warningMsg, void* extraInfo) override; // Listen for the 'onWarning' event	virtual void onEnterRoom(int result) override; // Listen for the 'onEnterRoom' event	virtual void onExitRoom(int reason) override; // Listen for the 'onExitRoom' event     // Get the TRTCCloud instance    static CRTCWindowsApp* GetInstance()	{		return static_cast<CRTCWindowsApp*>(AfxGetApp());	} // Implementation	DECLARE_MESSAGE_MAP()public:	ITRTCCloud* trtc_cloud_; // Declare the TRTCCloud member variable};
```

> **Примечание:** Название проекта, созданного в этой статье, это **RTCWindows**. Замените его на имя вашего собственного проекта или класса во всем процессе интеграции.

2. Инициализируйте экземпляр TRTC, вызвав `getTRTCShareInstance` в функции `InitInstance` файла **'PROJECTNAME'.h**.

```
// Initialize the TRTCCloud instance and add event listeningtrtc_cloud_ = getTRTCShareInstance();trtc_cloud_->addCallback(this);
```

> **Примечание:** Рекомендуется добавить код, инициализирующий экземпляр TRTC, после метода `SetRegistryKey(_T("Local AppWizard-Generated Applications")) `.

3. Реализуйте объявленные события прослушивания.

```
// Handle the listening eventsvoid CRTCWindowsApp::onError(TXLiteAVError errCode, const char* errMsg, void* extraInfo) {	// Listen for the "onError" event and print the corresponding information}void CRTCWindowsApp::onWarning(TXLiteAVWarning warningCode, const char* warningMsg, void* extraInfo) {	// Listen for the "onWarning" event and print the corresponding information}void CRTCWindowsApp::onEnterRoom(int result) { // Listen for the "onEnterRoom" event and print the corresponding information	if (result > 0) {		MessageBox(NULL, TEXT("Enter room successfully"), TEXT("Notice"), MB_OK | MB_ICONINFORMATION); 	}	else {		MessageBox(NULL, TEXT("Enter room failed"), TEXT("Notice"), MB_OK | MB_ICONINFORMATION); 	}}void CRTCWindowsApp::onExitRoom(int reason) { // Listen for the "onExitRoom" event and print the corresponding information    if (reason == 0) {        MessageBox(NULL, TEXT("Exit current room by calling the 'exitRoom' api of sdk"), TEXT("Notice"), MB_OK | MB_ICONINFORMATION);     } else if (reason == 1) {        MessageBox(NULL, TEXT("Kicked out of the current room by server through the restful api"), TEXT("Notice"), MB_OK | MB_ICONINFORMATION);     } else if (reason == 2) {        MessageBox(NULL, TEXT("The current room is dissolved by server through the restful api"), TEXT("Notice"), MB_OK | MB_ICONINFORMATION);     }}
```

### Шаг 4. Вход в комнату

Добавьте компонент **Button** в файл ресурсов **IDD_TRTCDEMO_DIALOG**, дважды щелкните вновь созданную кнопку. Затем установите параметр входа `TRTCParams` и вызовите `enterRoom` для успешного входа в комнату, что обычно вызывается после нажатия кнопки **Start Call**.

| Параметр | Тип | Описание |
| --- | --- | --- |
| sdkAppId | number | sdkAppId приложения аудио и видео, созданного в [TRTC Console](https://trtc.io/login?s_url=https://console.trtc.io/). |
| userId | string | Идентификатор пользователя, указанный вами. |
| userSig | string | Подпись пользователя, см. [UserSig](https://trtc.io/document/35166?platform=windows&product=rtcengine&menulabel=sdk). |
| roomId | number | Идентификатор комнаты, указанный вами, обычно уникальный идентификатор комнаты. |

Для более подробного описания параметров см. документ интерфейса [enterRoom](https://trtc.io/zh/document/50770#b6eb951dc67569848a415ba028f6746d).

```
void CRTCWindowsApp::OnBnClickedButton() {    // Modify the following trtcParams parameters to your own    liteav::TRTCParams trtcParams;    trtcParams.sdkAppId = 1400000123;    trtcParams.userId = "denny";     trtcParams.userSig = "xxx";    trtcParams.roomId = 123321;    // For multi-party video calls, `TRTC_APP_SCENE_LIVE` is recommended    trtc_cloud_->enterRoom(trtcParams, liteav::TRTCAppSceneLIVE);}
```

### Шаг 5. Включение/отключение камеры

1. В файле ресурсов **IDD_TRTCDEMO_DIALOG** нажмите **Toolbox** в левой границе и добавьте **Picture Control** в диалоговое окно.
2. Выберите **Properties** из контекстного меню и выберите **AFX_IDC_PICTURE** для ID.
3. Добавьте компонент **Button** в файл ресурсов **IDD_TRTCDEMO_DIALOG**, дважды щелкните вновь созданную кнопку. Инициализируйте **pLocalVideoView** и установите параметр рендеринга `setLocalRenderParams` для локального предпросмотра, затем вызовите `startLocalPreview` для локального предпросмотра. После успешного вызова `enterRoom` начнется передача потока.

```
void CRTCWindowsApp::OnBnClickedButton(){	// Set local preview rendering parameters	liteav::TRTCRenderParams render_params;	render_params.mirrorType = liteav::TRTCVideoMirrorType_Enable;	render_params.fillMode = TRTCVideoFillMode_Fill;	ITRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;	trtcCloud->setLocalRenderParams(render_params);	// Initialize pLocalVideoView	CWnd* pLocalVideoView = GetDlgItem(AFX_IDC_PICTURE);	if (pLocalVideoView != nullptr) {		auto local_view = (liteav::TXView)(pLocalVideoView->GetSafeHwnd());        // Preview the collected content locally 		trtcCloud->startLocalPreview(local_view);	}}
```

Вызовите `stopLocalPreview`, чтобы выключить предпросмотр камеры и остановить передачу локальной видеоинформации.

```
void CRTCWindowsApp::OnBnClickedButton(){    TRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;    trtcCloud->stopLocalPreview();}
```

### Шаг 6. Включение/отключение микрофона

Вызовите `startLocalAudio`, чтобы включить захват микрофона. **Выберите один из следующих параметров** `Quality` **звука в зависимости от ваших требований**.

```
// Enable microphone acquisition and set the current scene to: Voice mode // For high noise suppression capability, strong and weak network resistanceITRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;trtcCloud->startLocalAudio(TRTCAudioQualitySpeech);
```

```
// Enable microphone acquisition, and set the current scene to: Music mode // For high fidelity acquisition, low sound quality loss, recommended to use with professional sound cardsITRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;trtcCloud->startLocalAudio(TRTCAudioQualityMusic);
```

Вызовите `stopLocalAudio`, чтобы выключить захват микрофона и остановить передачу локальной аудиоинформации.

```
ITRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;trtcCloud->stopLocalAudio();
```

### Шаг 7. Воспроизведение/остановка видеопотока

1. Слушайте [onUserVideoAvailable](https://trtc.io/document/50771#6d1cb6b1fcf292ac6f34cd8baa) перед входом в комнату. Когда вы получите уведомление `onUserVideoAvailable(userId, true)`, это означает, что доступны видеокадры для воспроизведения на экране.
2. Вызовите `startRemoteView/stopRemoteView` для воспроизведения или остановки удаленного видео.

```
// Play the videoCWnd* pLocalVideoView = GetDlgItem(AFX_IDC_PICTURE);if (pLocalVideoView != nullptr) {    auto local_view = (liteav::TXView)(pLocalVideoView->GetSafeHwnd());    ITRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;    trtcCloud->startRemoteView("denny", liteav::TRTCVideoStreamTypeBig, video_view); // Start playing denny's video}// Stop the videoITRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;trtcCloud->stopRemoteView("denny", liteav::TRTCVideoStreamTypeBig); // Stop playing denny's videotrtcCloud->stopAllRemoteView(); // Stop all videos
```

### Шаг 8. Воспроизведение/остановка аудиопотока

Вызовите `muteRemoteAudio`, чтобы отключить или включить звук удаленной стороны.

```
// MuteITRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;trtcCloud->muteRemoteAudio("denny", true);  // Mute dennytrtcCloud->muteAllRemoteAudio(true); // Mute all remote users
```

```
// UnmuteITRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;trtcCloud->muteRemoteAudio("denny", false); // Unmute dennytrtcCloud->muteAllRemoteAudio(false); // Unmute all remote users
```

### Шаг 9. Выход из комнаты

Добавьте компонент **Button** в файл ресурсов **IDD_TRTCDEMO_DIALOG**, дважды щелкните вновь созданную кнопку. Вызовите `exitRoom` для выхода из текущей комнаты, и TRTC SDK уведомит вас после выхода через событие обратного вызова `onExitRoom`.

```
// Exit current roomvoid CRTCWindowsApp::OnBnClickedButton(){	ITRTCCloud* trtcCloud = CRTCWindowsApp::GetInstance()->trtc_cloud_;    trtcCloud->exitRoom();}
```

## Часто задаваемые вопросы

Справочник API доступен в [API Reference](https://trtc.io/document/35131?platform=windows&product=rtcengine).

Если вы сталкиваетесь с какими-либо проблемами при интеграции и использовании, обратитесь к [Часто задаваемые вопросы](https://trtc.io/document/36058?platform=windows&product=rtcengine).

## Свяжитесь с нами

Если у вас есть какие-либо предложения или замечания, пожалуйста, свяжитесь с нами по адресу `info_rtc@tencent.com`.


---
*Источник: [https://trtc.io/document/62042](https://trtc.io/document/62042)*

---
*Источник (EN): [integration.md](интеграция.md)*
